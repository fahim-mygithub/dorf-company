# Godot 4
.godot/
.import/
export.cfg
*.translation

# Build output (web export, etc.) — regenerated by CI
/build/

# Godot mono / .NET (if ever added)
.mono/
data_*/
mono_crash.*.json

# Paid software — DO NOT commit/redistribute. Your licensed copies stay local.
/godot-mcp-pro-v1.15.0/
/addons/godot_mcp/
/server/
node_modules/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*~

# Screenshots captured during agent sessions (regenerated, not source)
user://*.png
/screenshots/

# NOTE: export_presets.cfg IS committed (CI needs the Web preset) — it is intentionally not ignored.

# Research artifacts — internal reference only, never shipped (third-party product UI)
visual-corpus/
scholar-build/
